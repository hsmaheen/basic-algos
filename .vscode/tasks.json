{
  "version": "2.0.0",
  "tasks": [
    {
      "type": "process",
      "label": "build",
      "command": "dotnet",
      "args": ["build"],
      "options": {
        "cwd": "${workspaceRoot}"
      },
      "problemMatcher": "$msCompile"
    }
  ]
}
